name: dtlr-poc
channels:
  - nvidia/label/cuda-11.8.0
  - conda-forge
dependencies:
  - python=3.11.0
  - pip=24.3.1
  - setuptools=75.6.0
  - wheel=0.45.1
  - ninja=1.12.1
  # Compiler and development libraries for the custom PyTorch CUDA operators.
  # This intentionally shadows any newer system CUDA toolkit.
  - cuda=11.8.0
